/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://samplepaper.arbab.shop/', // Replace with your site's URL
    generateRobotsTxt: true,           // Generate robots.txt file
    exclude: ['/admin/*'],             // Exclude specific routes if needed
  };
  